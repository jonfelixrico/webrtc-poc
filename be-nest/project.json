{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "be-nest",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    }
  }
}
